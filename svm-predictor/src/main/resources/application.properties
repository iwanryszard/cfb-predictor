db.url=${db.url}
db.username=${db.username}
db.password=${db.password}
db.driverClassName=${db.driverClassName}

hibernate.dialect=${hibernate.dialect}
hibernate.show_sql=${hibernate.show_sql}
hibernate.hbm2ddl.auto=${hibernate.hbm2ddl.auto}

cfb.stats.root.url=${cfb.stats.root.url}

distance.matrix.url=${distance.matrix.url}
google.api.key=${google.api.key}

book.values.root.url=${book.values.root.url}
book.values.sbrSession=${book.values.sbrSession}
book.values.bbuserid=${book.values.bbuserid}
book.values.bbpassword=${book.values.bbpassword}
